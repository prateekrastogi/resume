hist(rnorm(1000))  # generate 100 N(0,1) variates and plot their histogram
hist(rnorm(1000))  # generate 100 N(0,1) variates and plot their histogram
dev.off()  # close the graphical output file
dev.off()  # close the graphical output file
pdf("xh.pdf")  # set graphical output file
hist(mean(abs(rnorm(1000000))))  # generate 100 N(0,1) variates and plot their histogram
dev.off()  # close the graphical output file
pdf("xh.pdf")  # set graphical output file
hist(mean(abs(rnorm(1000000))))  # generate 100 N(0,1) variates and plot their histogram
dev.off()  # close the graphical output file
pdf("xh.pdf")  # set graphical output file
pdf("xh.pdf")  # set graphical output file
hist(abs(rnorm(1000000)))  # generate 100 N(0,1) variates and plot their histogram
dev.off()  # close the graphical output file
pdf("xh.pdf")  # set graphical output file
pdf("xh.pdf")  # set graphical output file
hist(rnorm(100))  # generate 100 N(0,1) variates and plot their histogram
dev.off()  # close the graphical output file
pdf("pornh.pdf")  # set graphical output file
hist(abs(rnorm(100))  # generate 100 N(0,1) variates and plot their histogram
dev.off()  # close the graphical output file
pdf("pornh.pdf")  # set graphical output file
hist(mean(rnorm(100))  # generate 100 N(0,1) variates and plot their histogram
dev.off()  # close the graphical output file
pdf("pornh.pdf")  # set graphical output file
hist((rnorm(10000))  # generate 100 N(0,1) variates and plot their histogram
dev.off()  # close the graphical output file
hist(rnorm(10000))  # generate 100 N(0,1) variates and plot their histogram
pdf("xh.pdf")  # set graphical output file
hist(rnorm(100))  # generate 100 N(0,1) variates and plot their histogram
dev.off()  # close the graphical output file
pdf("xh.pdf")  # set graphical output file
hist(rnorm(100))  # generate 100 N(0,1) variates and plot their histogram
dev.off()  # close the graphical output file
pdf("xh.pdf")  # set graphical output file
hist(rnorm(10000))  # generate 100 N(0,1) variates and plot their histogram
invisible(dev.off())  # close the graphical output file
library("parallel", lib.loc="C:/Program Files/R/R-2.15.3/library")
detach("package:parallel", unload=TRUE)
